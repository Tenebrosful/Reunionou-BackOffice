{
  "name": "reunionou-backoffice",
  "scripts": {
    "eslint": "npx eslint . --ext .js --fix",
    "stylelint-css": "npx stylelint **/*.css --fix",
    "html-validate": "npx html-validate ."
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "eslint": "^8.11.0",
    "html-validate": "^6.6.0",
    "nodemon": "^2.0.15",
    "stylelint": "^14.0.1",
    "stylelint-config-standard": "^25.0.0"
  }
}
